import React from 'react';
import { createRoot } from 'react-dom/client';
import Extrato from './Extrato';

const rootElement = document.getElementById('root');

createRoot(rootElement).render(<Extrato />);
